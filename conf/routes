# File: routes
# Name: Peter Graham
# Class: CS 461
# Project 1
# Date: April 16

# Routes
# This file defines all application routes (Higher priority routes first)

# Home page
GET / Home.defaultFilters
GET /you/{order} Home.youFilter
GET /everyone/{order} Home.everyoneFilter

# Topic
GET /createtopic Topic.create
GET /vote Topic.onVoteSubmit
GET /topic/{topicName} Topic.index

# Profile viewing/modifying
GET /user/{id} Profile.index
GET /user/{id}/topics Profile.topics

# Static files
GET /static/ staticDir:public
GET /favicon.ico StaticContent.favicon
GET /robots.txt StaticContent.robots

# Catch-all
GET /{controller} {controller}.index
* /{controller}/{action} {controller}.{action}